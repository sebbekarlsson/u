# U
> Open a new `urxvt` terminal in the current directory.

## Usage
> How to use it:

    $ u

> (Just run `u`)

## Install
> To install it:

    $ sudo ./install.sh

> (Just execute the `install.sh` script)
